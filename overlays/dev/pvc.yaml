apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: reporting-pvc-dev
spec:
  storageClassName: nasoft-nfs-storage
  resources:
    requests:
      storage: 2Gi
  volumeMode: Filesystem
  accessModes:
    - ReadWriteOnce
